type Post @model {
	id: ID!
	title: String!
	ownerID: String!
	description: String
	programCode: String
	classCode: String
	book: Book @connection(name: "BookPosts")
}

type Book @model {
	id: ID!
	title: String!
	posts: [Post!] @connection(name: "BookPosts")
}